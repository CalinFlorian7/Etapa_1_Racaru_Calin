#include <iostream>
#include <string>
#include <ctime>
#include "Bilete.h"
#include "Eveniment.h"
#include "DetaliiEveniment.h"
#define _CRT_SECURE_NO_WARNINGS
using namespace std;


int main()
{
	/*DetaliiEveniment a, b;
	int* v;
	v = new int[3];
	for (int i = 0; i < 3; i++)
		v[i] = i;
	cin >> a;
	cout << a.get_numar_locuri_maxim();
	cout <<endl;
	a + (5);
cout << a;*/
/*string a;
char b;
cout << typeid(a).name()<<endl<< endl << endl << endl;
cout << typeid(b).name() ;*/
/*Bilete a;
cout << a.get_pret_redus() << endl;
cout<<a.operator int();*/
/*string a = "aaa";
string b = "aaa";*/
/*Eveniment a, b;
cin >> a >> b;
bool rezultat = a.operator==(b);
cout << rezultat;*/
	/*Eveniment a;
	cout << a.get_dim_denumire();
	cout << endl;
	a.operator++(5);
	cout << a.get_dim_denumire();*/
	/*int** v;
	int dim = 3;
	v = new int* [dim];
	int* vector;
	vector = new int[dim];

	for (int k = 0; k < dim; k++)
		cin >> vector[k];
	for (int i = 0; i < dim; i++)
		v[i] = new int[vector[i]];
	for (int i = 0; i < dim; i++)
		for (int j = 0; j < vector[i]; j++)
			v[i][j] = j + 1;

	for (int i = 0; i < dim; i++)
	{
		for (int j = 0; j <vector[i]; j++)
			cout << v[i][j] << " ";
		cout << endl;
	}*/
	/*DetaliiEveniment a;
	cin >> a;
	bool rez =a.verifica_locuri_libere();
	cout << rez;*/
	/*int i, j;
	i = 0;
	j = 0;
	int ok = 0;
	while (i < 10 && ok == 0)
	{
		while (j < 10 && ok == 0)
		{
			if (i+j== 5)
				ok = 1;
			cout << i << j << endl;
			j++;
		}
		i++;
	}*/
	
	/*DetaliiEveniment d;
	cin >> d;*/

	//b.extrage_loc_bilet_pe_zona(d, zona);
	//cout << b << endl;
	





}